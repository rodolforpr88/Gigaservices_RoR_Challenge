<% if resource.errors.any? %>
  <div class="card border-danger mb-3">
    <div class="card-header"><%= pluralize(resource.errors.count, "error") %> prohibited this resource from being saved:</div>
    <div class="card-body text-danger">
      <% resource.errors.each do |error| %>
        <p class="card-text"><%= error.full_message %></p>
      <% end %>
    </div>
  </div>
<% end %>
